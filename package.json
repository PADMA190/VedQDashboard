{
  "name": "student-quiz-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "Production-grade MERN Student Quiz Practice Dashboard for an EdTech platform.",
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "test": "npm run test --prefix backend",
    "format": "prettier --write \"**/*.{js,jsx,json,scss,md}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}